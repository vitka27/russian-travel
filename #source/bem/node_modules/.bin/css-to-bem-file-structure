#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../css-to-bem-file-structure/bin/create-by-css" "$@"
  ret=$?
else 
  node  "$basedir/../css-to-bem-file-structure/bin/create-by-css" "$@"
  ret=$?
fi
exit $ret
